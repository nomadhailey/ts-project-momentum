import React from "react";
import { StyledDiv } from "./Profile.style";
import { themeProps } from "./ModalMain";

export default function Profile({ theme, clickDark, clickLight }: themeProps) {
  return <StyledDiv>프로필</StyledDiv>;
}
